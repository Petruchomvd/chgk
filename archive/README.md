# Archive

This directory holds code that is intentionally preserved for reference but is
not part of the active product path.

- `legacy_parser/`: the old Selenium-based pack parser stack.
- `ad_hoc/`: one-off local scripts that were previously cluttering the repo root.
- `analytics_experiments/`: disposable analytics helpers that are not part of
  the maintained `Analytics/` package.
